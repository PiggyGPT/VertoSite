{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Dev Server",
            "type": "node-terminal",
            "request": "launch",
            "command": "npx vite --open",
            "cwd": "${workspaceFolder}"
        }
    ]
}